version: '3.9'
services:
  api:
    build: .
    ports:
      - '3333:3333'
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - NODE_ENV=development
      - PORT=3333
    volumes:
      - .:/app
      - /app/node_modules
    command: npm run dev
