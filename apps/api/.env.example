# OpenAI
OPENAI_API_KEY=sk-your-key-here

# App
PORT=3333
NODE_ENV=development

# CORS — domínio do app em produção
ALLOWED_ORIGINS=http://localhost:8081,exp://localhost:8081

# Rate limiting (requisições por IP por minuto)
RATE_LIMIT_TTL=60
RATE_LIMIT_MAX=10
